// I can't physically name it 'model' until it's inside controller! but still this definitions belongs only to peerRating and not to anything else. let's rename controller to bots? TODO
// import session from storage/session
// import models from storage/models

// export items, rates = models.define('items'), models.define('rates')

// export aspectToRate = session.define('aspectToRate')
